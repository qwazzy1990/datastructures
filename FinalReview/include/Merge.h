#ifndef _MERGE_
#define _MERGE_

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int* merge(int* left, int* right);
int* mergeSort(int* arr);
int* getPartition(int* arr, int start, int end);

#endif
